<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backend.user.mapper.userMapper">
    <select id="getUser" resultType="UserModel" parameterType="UserModel" statementType="CALLABLE">
        {
            call SP_USER_SELECT(
                @USER_ID = #{userId, mode=IN, jdbcType=NVARCHAR},
                @USER_PW = #{userPw, mode=IN, jdbcType=NVARCHAR}
            )
        }
    </select>
</mapper>